# OpenEA product screenshots

Replace these slots with final OpenEA screenshots:

- `repository.png` — recommended ~1600×1000, repository/object-detail view.
- `blast-radius.png` — recommended ~1200×800, Blast Radius analysis.
- `findings.png` — recommended ~1200×800, findings/rules view.

The current homepage uses styled placeholders rather than image tags so the site never ships broken images. When screenshots are ready, replace each `.screenshot-placeholder` block in `index.html` with an `<img>` pointing to the corresponding file. Keep the surrounding `<figure>`, browser chrome, and `<figcaption>`.
